let myView = document.getElementById('myView');
let transition = offsetTransition(100, 50); // Offset by 100 pixels horizontally and 50 pixels vertically
// Apply the transition to the view (implementation depends on the framework or context)
myView.applyTransition(transition);
